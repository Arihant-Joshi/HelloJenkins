Jenkinsfile (Declarative Pipeline)
pipeline {
    agent all
    stages {
        stage('build') {
            steps {
                sh '''
                	python ./hi.py > output.txt
                '''
                println "Build Step Complete"
            }
        }
    }
}