Jenkinsfile (Declarative Pipeline)
pipeline {
    agent all
    stages {
        stage('build') {
            steps {
                sh '''
                	echo "This is a console message"
                	python --version
                '''
            }
        }
    }
}